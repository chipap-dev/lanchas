[pytest]
DJANGO_SETTINGS_MODULE = config.settings.lanchas_standalone
testpaths = tests
python_files = test_*.py
markers =
    smoke: pega contra las fuentes reales de las empresas; solo corre en el cron semanal, no en CI de push/PR.
